<UserControl x:Class="Projektmanagement_DesktopApp.Views.ProjectsOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Projektmanagement_DesktopApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Test Projekt View:" VerticalAlignment="Center" Margin="0,0,5,0"/>
        
            <TextBox Width="200" 
                     Height="25" 
                     VerticalContentAlignment="Center"
                     Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}"/>
        
            <Button Content="Hinzufügen" 
                    Margin="10,0,0,0" 
                    Padding="10,2"
                    Command="{Binding AddCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
